+# داداش‌مد: وضعیت صادقانهٔ نسخهٔ اصلاح‌شده
+
+**این بسته، نسخهٔ آمادهٔ فیلم‌برداری نیست.** اصلاح مرحلهٔ اول روی کد ارسال‌شده انجام شده است. فایل `models/piper/fa_IR-gyro-medium.onnx` در ZIP ارسالی وجود نداشت؛ تنها `onnx.json` و چند WAV قدیمی موجود بودند. نه صدای زنانهٔ قابل‌اعتماد نصب شده، نه جنسیت مدل Gyro از متادیتا قابل تأیید است. خروجی ویدیو و رابط برنامه روی گوشی واقعی تست نشده‌اند. در این محیط نیز وابستگی‌های npm و موتورهای Piper/eSpeak در دسترس نبودند، پس build و تست اجرایی انجام نشد.
+
+## چه چیز در این مرحله تغییر کرد
+
+- مسیر اصلی گویندگی دیگر به `/api/voice/preview` که همیشه 404 می‌داد متکی نیست. دیالوگ از `/api/voice/synthesize` با موتور محلی درخواست می‌شود؛ در صورت فقدان مدل، **خطا برمی‌گردد و سکوت را موفقیت اعلام نمی‌کند**.
+- خروجی WAV گویندگی از Web Audio به بلندگو و ترک صدای ضبط متصل شده است. چند افکت داخلی هم اکنون از همان باس ضبط عبور می‌کنند. تأیید حضور صدا در فایل نهایی نیازمند ضبط و بازشنوی روی دستگاه مقصد است.
+- تشخیص نصب موتور از وضعیت واقعی فایل ONNX، JSON، باینری Piper/eSpeak و ffmpeg انجام می‌شود؛ نمایش «نصب‌شده/زنانه/تأییدشده» به‌صورت پیش‌فرض حذف شد.
+- گزارش موفقیت ساختگی در تست آفلاین و دیالوگ‌های WAV جعلی که در عمل بوق بودند حذف شدند. تست فعلی فقط تولید WAV در **سرور محلی** را می‌سنجد، نه قطع شبکه یا کیفیت جنسیت صدا.
+- متن دیالوگ دیگر در دستور shell اجرا نمی‌شود؛ شناسهٔ فایل WAV نیز محدود شده است. تأیید Cue فایل را واقعاً روی میزبان ذخیره می‌کند.
+- تمرین قبل از ضبط در صورت خرابی صدا وضعیت «آماده ضبط» نشان نمی‌دهد.
+
+## راه‌اندازی و تست، قبل از هر ضبط
+
+1. وابستگی‌های پروژه را با `npm install` روی دستگاه توسعهٔ دارای اینترنت نصب کنید؛ سپس `npm run lint` و `npm run build` را اجرا کنید. این بسته به دلیل فقدان دسترسی نصب در محیط ساخت حاضر به‌صورت عملی build نشده است.
+2. موتور Piper سازگار و `ffmpeg`/`ffprobe` را روی **همان دستگاهی که سرور Node برنامه را اجرا می‌کند** نصب کنید. مدل قانونی و مورد تأیید خودتان را با نام `models/piper/fa_IR-gyro-medium.onnx` کنار فایل JSON فعلی قرار دهید؛ برای صدای زنانه، **مدل زنانهٔ مجاز دیگری** همراه کانفیگ سازگار و تغییر نام/مسیر نصب لازم است. صدای Gyro در این بسته زنانه تأیید نشده است.
+3. `npm run dev` را اجرا کنید، صفحهٔ Voice Center را باز کنید و عبارت نمونه را واقعاً گوش دهید. `/api/voice/models/installed` باید نصب واقعی را گزارش کند. برای جایگزین کم‌کیفیت eSpeak، نصب `espeak-ng` روی میزبان لازم است.
+4. یک Cue را بسازید، با هدفون بشنوید، تأیید کنید، بعد یک کلیپ آزمایشی همراه گفتار و افکت ضبط و فایل نهایی را بازشنوی کنید. برنامه در نبود مدل، خطا می‌دهد؛ این رفتار عمدی است تا فیلم‌برداریِ بی‌صدا پنهان نشود.
+5. اینترنت را قطع کنید **اما سرور محلی را روشن نگه دارید**؛ صفحه و صدا و یک ضبط کامل را روی همان دستگاه آزمایش کنید. اپ فعلی PWA آفلاین واقعی و موتور نصب‌شدهٔ روی گوشی نیست: باز شدن صفحه روی گوشی به سرور میزبان نیاز دارد. اجرای مستقل روی گوشی مستلزم پورت WASM/PWA یا برنامهٔ بومی Android و تست ذخیره‌سازی است.
+
+## طراحی نسخهٔ تولیدی پیشنهادی
+
+برای هر اپیزود یک بستهٔ قابل‌نسخه‌بندی از سناریو، دفتر کارگردانی، راندها، دیالوگ‌ها، قوانین، Cueهای WAV تأییدشده، فایل زیرنویس فارسی UTF-8، تم و لوگوی شفاف بسازید. میان‌برهای اتاق فرمان باید رویدادهای ثبت‌شونده و برگشت‌پذیر داشته باشند؛ هر مرحلهٔ معرفی، یک صحنهٔ تمام‌صفحهٔ 16:9 با دستورالعمل و زمان‌بندی انیمیشن و متن زیرنویس مستقل باشد. لوگوی موجود در این سورس یک کامپوننت ترسیمی است، نه فایل لوگوی جداشدنی از تصویر؛ برای بازسازی برند، اصل فایل لوگو و حق استفاده از آن لازم است.
+
+کلید API را در مرورگر/LocalStorage قرار ندهید؛ آن را فقط در سرویس محلی امن و خارج از فایل‌های خروجی ذخیره کنید. در زمان ضبط، مدل زبانی باید اختیاری باشد و تصمیم داوری، امتیاز و گفتار حیاتی نباید به اتصال اینترنت وابسته بماند.
+
+خروجی مرورگر `MediaRecorder` به codec و سخت‌افزار وابسته است؛ عنوان 1080p60 تضمین توان سخت‌افزار یا کیفیت YouTube نیست. اول `isTypeSupported` و تست عملی ضبط، سپس خروجی نهایی H.264/AAC با `ffmpeg` و بازبینی تصویر/صدا. نسخهٔ فعلی فقط گرافیک canvas را ضبط می‌کند و ادعای رکورد کامل دوربین و ذخیرهٔ ابری ندارد.
+
+## منابع فنی
+
+- [MDN: MediaRecorder و محدودیت‌های codec](https://developer.mozilla.org/en-US/docs/Web/API/MediaRecorder)
+- [MDN: مسیر صدای قابل ضبط در Web Audio](https://developer.mozilla.org/en-US/docs/Web/API/AudioContext/createMediaStreamDestination)
+- [MDN: موجودی صدای مرورگر وابسته به دستگاه است](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis/getVoices)
+- [مدل‌های رسمی Piper و مجوز آن‌ها](https://github.com/OHF-Voice/piper1-gpl/blob/main/docs/VOICES.md)
+- [راهنمای رسمی فایل مناسب YouTube](https://support.google.com/youtube/answer/1722171)
+- [راهنمای W3C برای نوشتار فارسی/عربی](https://www.w3.org/TR/alreq/)
+- [فرمت زیرنویس قابل بارگذاری در YouTube](https://support.google.com/youtube/answer/2734698)
+
+**اولویت بعدی:** نمونهٔ صدای زنانهٔ فارسیِ مجاز + مدل قابل توزیع + تست حضوری روی گوشی و رایانه؛ بعد تثبیت ضبط، سپس بازطراحی کامل موشن و تم. بدون این سه، وعدهٔ «آفلاین و آمادهٔ فیلم‌برداری» قابل دفاع نیست.
